
Allow the base of the shared memory region to move.

Add all the unix time stuff.

Add popen and pclose.

Fix the fork bug.

Find out why the c include environs are broken.

Exec shells.

Check if an excessive amount of copying is done in the path handling routines.