global PERF_TEST
if { ![info exists PERF_TEST] || "$PERF_TEST" != "yes" } {
    return
}

load_lib mike-gcc.exp

gcc_init

prebase
set actions run
set compiler_output "^$"
set program_output "^$"
postbase sort2.c $run $groups

gcc_finish
