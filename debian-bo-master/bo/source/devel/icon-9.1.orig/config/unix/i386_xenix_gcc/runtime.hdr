#
# If you use nap instead of select add -lx to the ld flags variable.
# See the ./status file on the nap vs. select question.
#
LDFLAGS=-lx
CC=/usr/local/bin/gcc
CFLAGS= -O -fstrength-reduce -finline-functions
# CFLAGS= -g
SHELL=/bin/sh
LIBS=
