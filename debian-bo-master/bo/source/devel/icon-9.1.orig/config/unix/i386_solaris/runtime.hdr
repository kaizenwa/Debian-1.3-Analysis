LDFLAGS=
CC=cc
# changed optimization flag from -O to -xO1 -- seq(x) gives spurious 
# "integer overflow" runtime error for values less than -1 when compiled
# with Sun cc under Solaris 2.2
CFLAGS= -xO1 -I/usr/openwin/include 
SHELL=/bin/sh
