#!/bin/sh
#
# compute version number of first argument
#

if [ $# != 1 ]
then
	echo "Usage: package-version <package name>"
	exit 1
fi

dpkg --status $1 | egrep ^Version | awk '{print $2}' | awk -F- '{ print $1 }'

exit 0
