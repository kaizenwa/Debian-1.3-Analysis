Longer term projects:

1. re-indent the code with 'indent -bad -ncdb -d0 -nfc1 -i8 -l256 -sc'

2. autoconf/automake integration

3. get rid of all this alloc.c garbage?  use vmalloc?  require vmalloc?

4. add hooks to entertain sfio

5. re-write the admin guide

6. move to syslog logging where possible

7. fix mkaliases to determine ALIASES_TYPE (and ALIASES_FILE(s)?) at
   runtime
