***************
*** 25,32 ****
  
  SHLIB_FILE_MAJOR:=$(basename $(SHLIB_NAME)).so.$(SHLIB_VERSION_MAJOR)
  
- SYSLIBS:= $(SHARED_DIR)/libgcc/libgcc3.a ../libc/libtermcap.sa \
- 	../libc/libc.sa
  
  STUBNAMES=libcurses
  STUBLIBS:= $(foreach lib, $(STUBNAMES), $(lib).sa)
--- 29,36 ----
  
  SHLIB_FILE_MAJOR:=$(basename $(SHLIB_NAME)).so.$(SHLIB_VERSION_MAJOR)
  
+ SYSLIBS:= $(SHARED_DIR)/libgcc/libgcc3.a ../libc$(NYS_DIR_SUFFIX)/libtermcap.sa \
+ 	../libc$(NYS_DIR_SUFFIX)/libc.sa
  
  STUBNAMES=libcurses
  STUBLIBS:= $(foreach lib, $(STUBNAMES), $(lib).sa)
