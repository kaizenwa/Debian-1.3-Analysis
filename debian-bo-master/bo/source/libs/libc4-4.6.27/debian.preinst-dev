#!/bin/sh

set -e

# Remove old symlinks, if they exist
if [ -L /usr/i486-linuxaout/include/asm ]; then
	rm -f /usr/i486-linuxaout/include/asm
fi
if [ -L /usr/i486-linuxaout/include/linux ]; then
	rm -f /usr/i486-linuxaout/include/linux
fi

