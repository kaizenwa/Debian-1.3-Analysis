		     <!-- -*-jfw-sgml-*- -->
<!doctype manpage public "-//JFW//DTD Manpage//EN"[
<!ENTITY % f.text system "common.ent" > %f.text;
<!ENTITY % remark "ignore" >
<!ENTITY sdc "sdc" >
]>

<manpage
date="19. 3. 1995"
author="J&oe;rg Wittenberger"
inst="TU Dresden"
>
&sdc;<short>an extensible SGML formatter
<synopsis>
<code/&sdc;/ 
<code/-o/ <var/outputfile/ 
[ <code/-O/ <var/format/ ] 
[ <code/-L/ <var/directory/ ]
[ <code/-i/ <var/directory/ ] 
[ <code/-m/ <var/file/ ] 
[ <code/-R/ <var/startup file/ ]
[ <code/-V/ <var/level/ ] 
<var/files/ ...
<descript>

&t.desc

<options>

&t.opts

<return>

&sdc; returns an exist value of 0 upon successful SGML parsing and 1
if the input does not conform to the DTD. Semantic errors like
unresolved references are currently not covered.

<examples>
&t.exam

<env>

&t.env

<files>

&t.files

<conform>
The underlying &sgmls; conforms to ISO 8879. Therefore &sdc; too.

<notes>
At the moment there is no complete developers documentation
available. Support for RTF output is still far from complete and uses
the old mechanism.

<diag>
An error

<code/SGML error at /<var/file/<code/, line /<var/number/

comes from the underlying SGML parser and indicates text not
conforming to the document type definition. Usually this will imply a
lot of other errors.

<code/*** ERROR:bigloo:eval:/

Indicates a bug in the format translation files.

<see>
<ref t=m id="sgmls(1)"//, <ref t=m id="lout(1)"//, <ref t=m
id="info(5)"//, <ref t=m id="latex(1)"//, <ref t=m id="xfig(1)"//.
