# debiandoc2<type> generates foo.<type> from foo[.sgml]
# using saspconvert <toc> <fnotes> to convert foo.sasp to foo.sasp-<sasp>
# sgmlspl spec-<spec> <argument>
# (if argument starts with `.' then it is a suffix, and
#  foo<argument> is passed instead)
#
# type	sasp	spec	toc	fnotes	argument
html	html	html	size	book	.html/
lout	lout	lout	opt	.
text	text	text	opt	p+	plain
textov	text	text	opt	p+	overstrike
