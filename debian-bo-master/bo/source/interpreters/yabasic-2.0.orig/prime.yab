rem
rem This program calculates prime-numbers ...
rem
for can=3 to 1000 step 2
  for div=3 to sqrt(can)
    if (frac(can/div)=0) then 
      goto nextcan
    fi
  next div
  print can," is a prime number"
  label nextcan
next cat

    
