##
## BUGS:
##

UID and GID creation will fail if there's a UID or GID one less than
that of "nobody" or "nogroup"

Check locking on password and group files

Httpd looks for icon files in "/usr/lib/cern-httpd" rather than
"/usr/lib/httpd".

Command-buffer input

Update postinst script to use "addgroup"

Update preinst, postinst and prerm to use "start-stop-daemon" to kill
daemon before replacement