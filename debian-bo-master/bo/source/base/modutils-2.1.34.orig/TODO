insmod:

	- Persistant module parameters.  
	- Program to display modinfo bits intelligably.

depmod:

	- Finalize MODULE_SUPPORTED_DEVICE info so that we can replace
	  the hardcoded alias array with info gleened from the modules
	  themselves.
