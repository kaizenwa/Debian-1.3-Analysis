# Makefile.in generated automatically by automake 1.0 from Makefile.am

# Copyright (C) 1994, 1995, 1996 Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy, distribute and modify it.


SHELL = /bin/sh

srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
prefix = @prefix@
exec_prefix = @exec_prefix@

bindir = @bindir@
sbindir = @sbindir@
libexecdir = @libexecdir@
datadir = @datadir@
sysconfdir = @sysconfdir@
sharedstatedir = @sharedstatedir@
localstatedir = @localstatedir@
libdir = @libdir@
infodir = @infodir@
mandir = @mandir@
includedir = @includedir@
oldincludedir = /usr/include

pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@

top_builddir = ..

INSTALL = @INSTALL@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
transform = @program_transform_name@

AUTOMAKE_OPTIONS = 1.0 foreign

man_MANS = chage.1 chfn.1 chsh.1 gpasswd.1 \
 login.1 newgrp.1 passwd.1 su.1 \
 pw_auth.3 shadow.3 \
 faillog.5 login.access.5 login.defs.5 passwd.5 \
 porttime.5 shadow.5 suauth.5 \
 chpasswd.8 dpasswd.8 faillog.8 \
 groupadd.8 groupdel.8 groupmod.8 \
 grpck.8 lastlog.8 \
 logoutd.8 mkpasswd.8 newusers.8 pwauth.8 pwck.8 \
 useradd.8 userdel.8 usermod.8

EXTRA_DIST = id.1 groups.1 pwconv.8 pwunconv.8 sulogin.8
mkinstalldirs = $(top_srcdir)/mkinstalldirs
CONFIG_HEADER = ../config.h
MANS = chage.1 chfn.1 chsh.1 gpasswd.1  login.1 newgrp.1 passwd.1 su.1  pw_auth.3 shadow.3  faillog.5 login.access.5 login.defs.5 passwd.5  porttime.5 shadow.5 suauth.5  chpasswd.8 dpasswd.8 faillog.8  groupadd.8 groupdel.8 groupmod.8  grpck.8 lastlog.8  logoutd.8 mkpasswd.8 newusers.8 pwauth.8 pwck.8  useradd.8 userdel.8 usermod.8


NROFF = nroff
DIST_COMMON = Makefile.am Makefile.in


PACKAGE = @PACKAGE@
VERSION = @VERSION@

DISTFILES = $(DIST_COMMON) $(SOURCES) $(BUILT_SOURCES) $(HEADERS) \
	$(TEXINFOS) $(INFOS) $(MANS) $(EXTRA_DIST) $(DATA)
DEP_DISTFILES = $(DIST_COMMON) $(SOURCES) $(BUILT_SOURCES) $(HEADERS) \
	$(TEXINFOS) $(INFO_DEPS) $(MANS) $(EXTRA_DIST) $(DATA)

TAR = tar
default: all


$(srcdir)/Makefile.in: Makefile.am $(top_srcdir)/configure.in
	cd $(top_srcdir) && automake $(subdir)/Makefile

Makefile: $(top_builddir)/config.status Makefile.in
	cd $(top_builddir) && CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= ./config.status
install-man: $(MANS)
	$(mkinstalldirs) $(mandir)/man8
	$(mkinstalldirs) $(mandir)/man1
	$(mkinstalldirs) $(mandir)/man3
	$(mkinstalldirs) $(mandir)/man5
	@sect=3;				\
	inst=`echo "shadow" | sed '$(transform)'`.3; \
	echo installing shadow.3 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/shadow.3 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "faillog" | sed '$(transform)'`.8; \
	echo installing faillog.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/faillog.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "faillog" | sed '$(transform)'`.5; \
	echo installing faillog.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/faillog.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "pwck" | sed '$(transform)'`.8; \
	echo installing pwck.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/pwck.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "groupadd" | sed '$(transform)'`.8; \
	echo installing groupadd.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/groupadd.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "login.access" | sed '$(transform)'`.5; \
	echo installing login.access.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/login.access.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "useradd" | sed '$(transform)'`.8; \
	echo installing useradd.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/useradd.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "groupdel" | sed '$(transform)'`.8; \
	echo installing groupdel.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/groupdel.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "login.defs" | sed '$(transform)'`.5; \
	echo installing login.defs.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/login.defs.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "userdel" | sed '$(transform)'`.8; \
	echo installing userdel.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/userdel.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "groupmod" | sed '$(transform)'`.8; \
	echo installing groupmod.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/groupmod.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "passwd" | sed '$(transform)'`.5; \
	echo installing passwd.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/passwd.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "usermod" | sed '$(transform)'`.8; \
	echo installing usermod.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/usermod.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "grpck" | sed '$(transform)'`.8; \
	echo installing grpck.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/grpck.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "porttime" | sed '$(transform)'`.5; \
	echo installing porttime.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/porttime.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "lastlog" | sed '$(transform)'`.8; \
	echo installing lastlog.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/lastlog.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "shadow" | sed '$(transform)'`.5; \
	echo installing shadow.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/shadow.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "logoutd" | sed '$(transform)'`.8; \
	echo installing logoutd.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/logoutd.8 $(mandir)/man$$sect/$$inst
	@sect=5;				\
	inst=`echo "suauth" | sed '$(transform)'`.5; \
	echo installing suauth.5 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/suauth.5 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "mkpasswd" | sed '$(transform)'`.8; \
	echo installing mkpasswd.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/mkpasswd.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "chpasswd" | sed '$(transform)'`.8; \
	echo installing chpasswd.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/chpasswd.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "newusers" | sed '$(transform)'`.8; \
	echo installing newusers.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/newusers.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "dpasswd" | sed '$(transform)'`.8; \
	echo installing dpasswd.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/dpasswd.8 $(mandir)/man$$sect/$$inst
	@sect=8;				\
	inst=`echo "pwauth" | sed '$(transform)'`.8; \
	echo installing pwauth.8 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/pwauth.8 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "chage" | sed '$(transform)'`.1; \
	echo installing chage.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/chage.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "chfn" | sed '$(transform)'`.1; \
	echo installing chfn.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/chfn.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "chsh" | sed '$(transform)'`.1; \
	echo installing chsh.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/chsh.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "gpasswd" | sed '$(transform)'`.1; \
	echo installing gpasswd.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/gpasswd.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "login" | sed '$(transform)'`.1; \
	echo installing login.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/login.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "newgrp" | sed '$(transform)'`.1; \
	echo installing newgrp.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/newgrp.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "passwd" | sed '$(transform)'`.1; \
	echo installing passwd.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/passwd.1 $(mandir)/man$$sect/$$inst
	@sect=1;				\
	inst=`echo "su" | sed '$(transform)'`.1; \
	echo installing su.1 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/su.1 $(mandir)/man$$sect/$$inst
	@sect=3;				\
	inst=`echo "pw_auth" | sed '$(transform)'`.3; \
	echo installing pw_auth.3 as $(mandir)/man$$sect/$$inst; \
	$(INSTALL_DATA) $(srcdir)/pw_auth.3 $(mandir)/man$$sect/$$inst

uninstall-man:
	inst=`echo "shadow" | sed '$(transform)'`.3; \
	rm -f $(mandir)/man3/$$inst
	inst=`echo "faillog" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "faillog" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "pwck" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "groupadd" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "login.access" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "useradd" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "groupdel" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "login.defs" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "userdel" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "groupmod" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "passwd" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "usermod" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "grpck" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "porttime" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "lastlog" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "shadow" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "logoutd" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "suauth" | sed '$(transform)'`.5; \
	rm -f $(mandir)/man5/$$inst
	inst=`echo "mkpasswd" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "chpasswd" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "newusers" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "dpasswd" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "pwauth" | sed '$(transform)'`.8; \
	rm -f $(mandir)/man8/$$inst
	inst=`echo "chage" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "chfn" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "chsh" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "gpasswd" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "login" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "newgrp" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "passwd" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "su" | sed '$(transform)'`.1; \
	rm -f $(mandir)/man1/$$inst
	inst=`echo "pw_auth" | sed '$(transform)'`.3; \
	rm -f $(mandir)/man3/$$inst

tags: TAGS
TAGS:


subdir = man
distdir = $(top_builddir)/$(PACKAGE)-$(VERSION)/$(subdir)
distdir: $(DEP_DISTFILES)
	@for file in `cd $(srcdir) && echo $(DISTFILES)`; do \
	  test -f $(distdir)/$$file \
	  || ln $(srcdir)/$$file $(distdir)/$$file 2> /dev/null \
	  || cp -p $(srcdir)/$$file $(distdir)/$$file; \
	done
info:

dvi:

check: all

installcheck:

install-exec: 

install-data: install-man

install: install-exec install-data all
	@:

uninstall: uninstall-man

all: $(MANS) Makefile

install-strip:
	$(MAKE) INSTALL_PROGRAM='$(INSTALL_PROGRAM) -s' install
installdirs:
	$(mkinstalldirs)  $(mandir)/man8 $(mandir)/man1 $(mandir)/man3 \
		$(mandir)/man5


mostlyclean-generic:
	test -z "$(MOSTLYCLEANFILES)" || rm -f $(MOSTLYCLEANFILES)

clean-generic:
	test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	rm -f Makefile $(DISTCLEANFILES)
	rm -f config.cache config.log $(CONFIG_HEADER) stamp-h

maintainer-clean-generic:
	test -z "$(MAINTAINERCLEANFILES)" || rm -f $(MAINTAINERCLEANFILES)
	test -z "$(BUILT_SOURCES)" || rm -f $(BUILT_SOURCES)
mostlyclean:  mostlyclean-generic

clean:  clean-generic mostlyclean 

distclean:  distclean-generic clean 
	rm -f config.status

maintainer-clean:  maintainer-clean-generic distclean 
	@echo "This command is intended for maintainers to use;"
	@echo "it deletes files that may require special tools to rebuild."

.PHONY: default install-man uninstall-man tags distdir info dvi check \
installcheck install-exec install-data install uninstall all \
installdirs mostlyclean-generic distclean-generic clean-generic \
maintainer-clean-generic clean mostlyclean distclean maintainer-clean

.SUFFIXES:

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
