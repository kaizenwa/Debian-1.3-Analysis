# This is what I use from Linux -> TitanOS (bcr)
# For a description of what the options mean, look at the TERMRC file.

baudrate 14400
collisions on
compress on
noise on
stopbits 0.5
terminate 'NO CARRIER'
timeout 200
