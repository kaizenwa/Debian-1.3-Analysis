# This is what I would try first of a 2400 baud line. (bcr)
# For a description of what the options mean, look at the TERMRC file.

baudrate 2400
compress on
noise off
stopbits 1
terminate 'NO CARRIER'
timeout 70
