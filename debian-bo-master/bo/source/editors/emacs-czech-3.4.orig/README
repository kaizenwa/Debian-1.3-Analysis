The following text is in Czech language.  If you are interested in it and
you don't understand Czech, send e-mail to `pdm@fi.muni.cz'.


CO JE EMACS-CZECH

emacs-czech je programový balík pro podporu psaní a zpracování èeských textù
v GNU Emacsu.  V souèasné dobì má tyto základní funkce:

 - Èeská klávesnice (plnì funkèní a ¹iroce konfigurovatelná).
 - Podpora psaní èeských textù.
 - Konverze znakových sad.
 - Automatické konverze mailù a newsù.
 - Jednoduché pseudoèeské tøídìní.

Podporován je Emacs od verze 19.30 vèetnì své NT verze a XEmacs od verze
19.14.  Pro zcela bezproblémový provoz je vy¾adován Emacs 19.31 nebo vy¹¹í
v unixové verzi.

Co vyhovuje mnì, nemusí vyhovovat jinému.  Budu proto velmi vdìèný za
pøipomínky, komentáøe, návrhy, námìty a zprávy o chybách.  I pouhé sdìlení, ¾e
tuto vìc nìkdo pou¾ívá, má pro mì význam -- moje budoucí snaha o rùzná
vylep¹ení bude pravdìpodobnì pøímo úmìrná pøedpokládanému poètu u¾ivatelù.

Dìkuji Jaroslavu ©najdrovi, Michalu Koneènému, Janu Kasprzakovi a Ale¹i Èepkovi
za podporu mé práce.


INSTALACE

Pøeètìte si prosím instalaèní instrukce peèlivì.  Popis instalace je obsa¾en i
v PostScriptové dokumentaci.

Poznámka: Zmiòovaný XEmacs *není* GNU Emacs v Xové podobì, nýbr¾ jiný druh
Emacsu.

Doporuèený postup instalace:

 - Zeditujte dle potøeby úvodní promìnné v `Makefile'.
 - Upgradujete-li ze star¹í verze, sma¾te v¹echny soubory této verze
   (dùle¾ité!).
 - Proveïte `make install' (warningy byte-kompilátoru Emacsu ignorujte).
 - Updatujte soubor `dir' v patøièném `info' adresáøi.
 - Do svého `~/.emacs' pøidejte øádky
     (setq load-path (cons "/cesta/k/adresáøi/s/emacs/czech" load-path))
     (load "czech")
   Instalujete-li `emacs-czech' globálnì, umístìte první øádek
   (nastavení cesty) do souboru `site-start.el'.  Neumis»ujte zde
   druhý øádek (zavedení èe¹tiny)!  Chcete-li, aby se èe¹tina
   zavádìla v¹em u¾ivatelùm, umístìte do souboru `default.el'
   (pokud v adresáøích Emacsu není, vytvoøte jej v adresáøi,
   kde se nachází `site-start.el') øádek
     (require 'czech)
 - Dále postupujte podle dokumentace.
 - V pøípadì, ¾e pou¾íváte XEmacs nebo mo¾ná i Emacs 19.30 z linuxovských
   distribucí Slackware nebo RedHat, nainstalujte i pøilo¾ený soubor
   `iso02-syn.el', nejlépe do *standardního* adresáøe lispovských souborù
   (X)Emacsu.

Pokud z nìjakého dùvodu `make' sel¾e, musíte provést patøièné kroky ruènì podle
¹ablony dané následujícím seznamem:

 1. Proveïte byte-kompilaci v¹ech souborù `czech*.el'.
 2. Zkopírujte vzniklé `*.elc' soubory do nìjakého `site-lisp' adresáøe.
 3. Soubory `emacs-czech.info*' zkopírujte do nìjakého adresáøe s info soubory
    (obvykle `/usr/local/info/').
 4. Chcete-li, mù¾ete tyto soubory zagzipovat (i pak budou pro Emacs èitelné).


DOKUMENTACE

Info dokumentace je v souborech `*.info*'.
Lze ji také poøídit pøíkazem `make info'.

PostSciptová dokumentace je v souboru `emacs-czech.ps' v rozli¹ení 600dpi.
Lze ji té¾ vyrobit pøíkazem `make ps'.

DVI dokumentaci lze poøídit pøíkazem `make dvi'.

Vyèistit adresáø lze pøíkazem `make clean'.
Zbavit se ve¹kerých generovaných souborù je mo¾né pøíkazem `make mrproper'.


SERVIS

Aktuální informace a nejnovìj¹í verze `emacs-czech' jsou k dispozici na
`http://www.fi.muni.cz/~pdm/emacs-czech.html'.

S pøípadnými dotazy a pøipomínkami se obracejte na adresu `pdm@fi.muni.cz'.


KOPÍROVÁNÍ A ZÁRUKA

Autorem programu je Milan Zamazal.
Program lze pou¾ívat pod podmínkami GNU General Public License, version 2.
Její plné znìní je v souboru `COPYING' a mìlo by se té¾ zobrazit po stisku
kláves `C-h C-c'.
Autor neposkytuje ¾ádnou záruku, podrobnosti se lze dozvìdìt v licenci nebo
i kombinací kláves `C-h C-w'.

Soubor `iso02-syn.el' je zkopírován z distribuce GNU Emacsu 19.30.
Soubor `logo.xpm' vznikl lokalizací ikony `gnu.xpm'.
Soubor `cztexinfo.tex' vznikl poèe¹tìním souboru `texinfo.tex'.  Vzhledem
k tomu, ¾e TeXu nerozumím, tak také podle toho tento soubor vypadá.

