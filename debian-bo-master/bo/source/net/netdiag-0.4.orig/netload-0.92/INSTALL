Read the README file for more information.

The installation is pretty simple

	- Type "make" to build the binary.
	- Type "make install" to install the binary (default /usr/local/bin)

If you decide to get rid of it, type "make clean" and it will remove
the binary system-wide.

That's it,
enjoy.

Luis
