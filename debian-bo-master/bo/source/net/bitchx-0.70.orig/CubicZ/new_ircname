#!/usr/bin/perl

open(FILE, "/home/edwards/.BitchX/irc_names");

$NumOfNames = <FILE>;
chop($NumOfNames);

srand(time());
$rannum = int(rand($NumOfNames))+1;

$RandomName = "";
for ($x = 1; $x <= $rannum; $x++) {
$RandomName = <FILE>;
}

print($RandomName);

close(FILE);
