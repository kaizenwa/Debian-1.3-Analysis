Mail netstd maintainer for inclusion of /etc/init.d/bootpd in his package

Mail netstd maintainer for updation of nfsd server.

Make an option for those that like to use lilo.
(I currently don't need it, but it should at least be possible!)


Write a few scripts/programmes for:
  I've got, say, client XX has got, say 60M local diskspace left.
  I want it to copy (for example overnight) copy the files
  it's accessing most frequently to this local disk, and fill
  the rest up with symlinks.
I'm not quite sure how I should handle this, but slightly modifying
nfsd so that it records what clients acces what files and with what
frequencies, should be all that is really needed. (And write a small
programme that runs on the clients and copies these files).

--------------------------------
#the stuff below I forgot to remove
--------------------------------

+test +debian pakket maken (installatie skriptjes schrijven)

+test /etc/init.d/nrprobenet ranges

+test +scripts voor in /etc/init.d/ (goed) maken

+maak /etc/init.d/boot script goed!!!!! (ook installatie
 var /etc/rc?.d/ links!)
 (haal /etc/init.d/boot weg, zet er een /etc/init.d/nrclientboot
 voor in de plaats; )
-haal deze (nederlandse!) file weg!

+conffiles werking testen

-bootpd in /etc/init.d, moet ik die opstarten tijdens installatie?

+directories in /etc met "cp -a" copyeren (voor /var van rulgme).

+doe een
    egrep -3 "^[^#]*(132\.229\.1|rul[^e])" `find . -type f -print` 
 voor release!


