

	New command added: "authorize" - inform remote ftp gateway server
that it should negotiate cryptokey authentication.

	If invoked as "gate-ftp", the environment variable FTPSERVER is
searched for, and is contacted as a proxy ftp gateway. Autologin in done
through the proxy. If FTPSERVERPORT is set, that is used as the port
number for the gateway server.

