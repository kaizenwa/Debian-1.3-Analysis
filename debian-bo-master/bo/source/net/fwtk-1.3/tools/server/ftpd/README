
	This is a port of the BSD ftpd with support for
the firewall auth server.

	This version also:
		a) prevents retrieves of the password file and logs attempts
		b) recognizes the proxy ftp-gw if compiled to do so
