# sample login-ts shell file
#
# normal users
# this should look somewhat like a password file so that things
# requiring a password file can work. the user's name is used if
# you are running rlogin, so make sure there is a 1:1 uid relationship
# also note some versions of getpwent expect a group field even if it
# is not being used.
root::0:0:::/bin/csh
avolio::122:0:::/bin/sh
mjr::122:0:::/bin/sh
