
ypbind:	ypbind.c
	cc -Wall -O2 -D__BSD_SIGNAL -DDAEMON -s -o ypbind ypbind.c -lbsd

install:
	install -m 755 -g root ypbind $(DEBDIR)/usr/sbin
	install -m 644 -g root ypbind.8 $(DEBDIR)/usr/man/man8

clean:
	-rm -f *.bak *.o ypbind

