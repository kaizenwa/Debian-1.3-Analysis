#!/bin/bash
fromfile=$1
todir=$2

if test -f "$fromfile" -a -d "$todir"; then
  (echo 'function f () { file=$1; shift; echo "$@">> '$todir'/$file } ';\
   sed -e "s/#.*$//" $fromfile|sed -e "/[^ ]/s/^/f /" )|sh


else
  echo "usage: $0 fromfile todir"
fi
   