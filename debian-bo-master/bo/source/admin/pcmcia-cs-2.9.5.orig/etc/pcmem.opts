# Device configuration for the "old" memory card driver
#
# The address is "scheme,socket".
#
case "$ADDRESS" in
*,*)
    #DO_FSTAB="y" ; DO_MOUNT="y"
    #FSTYPE="msdos"
    #OPTS=""
    #MOUNTPT="/mem"
    ;;
esac
