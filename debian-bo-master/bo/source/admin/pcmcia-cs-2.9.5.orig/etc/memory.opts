# Device configuration for the "new" memory card driver
#
# The address format is "scheme,socket,instance".
#
case "$ADDRESS" in
*,*,0)
    #DO_FSTAB="y" ; DO_FSCK="y" ; DO_MOUNT="y"
    #FSTYPE="ext2"
    #OPTS=""
    #MOUNTPT="/mem"
    ;;
esac
