
A distributed cron daemon with centralized crontab files. Use socket on cfengine
port to serve these? Contains name of machine as well as name of script...

ordering of resolv addresses if host is in list place it first. But set
an option which switches this on and off.

fix the dir struture and makefiles

A way to change the net interface from the config file, overriding
the default value per class.

Can you iterate over a list in a shell command - eg rsh $(hosts) balbla

Run script if file is copied but not otherwise
Or define internal-class so that other actions can be enabled!!! Better!

  copy:

         define=class,list,class,list  undefine=class,lists,

   use : or , to separate

