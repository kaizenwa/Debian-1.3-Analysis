version 0.5:
	* `screen.c' is still pretty crude, won't handle menus longer than the screen properly.
	* man page started, still rough around the edges.
	* needs a better readme
	* `Manual' is completely incomplete.
