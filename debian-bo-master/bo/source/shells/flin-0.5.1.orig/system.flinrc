# This is the system.flinrc

# We are (hopefully) running a secure shell. Restrict the flin options
# available in the users' .flinrc and .flinlogin files

restrict all except pause noclobber

# Turn on exec logging
logging on

