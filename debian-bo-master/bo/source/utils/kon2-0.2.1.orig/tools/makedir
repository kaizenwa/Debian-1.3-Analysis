#!/bin/sh

echo -n 'checking' $1 '...'

if [ -d $1 ]
then
	echo 'ok'
else
	echo ' not exist'
	echo -n 'make' "'$1'" '? '
	read yn
	if [ "$yn" = "n" -o "$yn" = "N" ]
	then
		exit 1
	fi
	mkdir -p $1
	if [ $? != 0 ]
	then
		exit 1
	fi
fi
