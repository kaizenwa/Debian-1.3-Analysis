dnl Process this file with autoconf to produce a configure script.
AC_INIT(time.c)

AC_PROG_CC
AC_PROG_CPP
AC_PROG_INSTALL

AC_MINIX
AC_ISC_POSIX

AC_SIZE_T
AC_PID_T
AC_RETSIGTYPE
AC_CONST

AC_STDC_HEADERS
AC_HAVE_HEADERS(unistd.h string.h sys/wait.h sys/rusage.h)
AC_HEADER_EGREP(w_status, sys/wait.h, AC_DEFINE(HAVE_UNION_WAIT))
AC_HEADER_EGREP(tv_usec, sys/time.h, AC_DEFINE(HAVE_TIMEVAL))
AC_WAIT3

AC_COMPILE_CHECK(getpagesize, [#include <sys/param.h>],
[getpagesize();], AC_DEFINE(HAVE_GETPAGESIZE))

AC_HAVE_FUNCS(strerror)
AC_VPRINTF

AC_OUTPUT(Makefile)
