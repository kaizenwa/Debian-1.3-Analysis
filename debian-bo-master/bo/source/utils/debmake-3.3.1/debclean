#!/bin/sh

# Script to clean up debian directories
for i in `find . -name "debian"`; do
	DIR=`expr $i : "\(.*\)/[^/]*"`
	echo $DIR
	cd $DIR
	# Clean up the source package
	build clean
	cd ..
	# Clean up the package related files
	rm -f *.dsc *.changes *.deb
done
