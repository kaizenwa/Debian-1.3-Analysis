
wm withdraw .

if {[lsearch [winfo interps] "tkdesk"] < 0} {
    puts "Please start tkdesk first."
    exit 1
}

foreach file $argv {
    if {[string index $file 0] != "/"} {
    	set file [pwd]/$file
    }
    lappend files $file
}

eval send tkdesk dsk_edit $files
exit 0
