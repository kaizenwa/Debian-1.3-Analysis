
wm withdraw .

if {[lsearch [winfo interps] "tkdesk"] < 0} {
    puts "Please start tkdesk first."
    exit 1
}

if {$argv == ""} {
    puts "Usage: dod <path>"
} else {
    if {$argv == "."} {
	send tkdesk dsk_open_dir [pwd]
    } else {
	send tkdesk dsk_open_dir [lindex $argv 0]
    }
}
exit 0
