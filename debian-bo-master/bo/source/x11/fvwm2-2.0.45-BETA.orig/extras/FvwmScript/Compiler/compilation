flex source/lexin
bison -d -y source/bisonin
gcc y.tab.c lex.yy.c -ll

A ajouter a la fin de  y.tab.c

int yyerror(char *errmsg)
{
 fprintf(stderr, "%s\n",errmsg);
 return 0;
}


vers la ligne 626 dans y.tab.c
 - enlever tout ce qui concerne alloca
 - le remplacer par:

void *alloca (unsigned int a)
{
}

