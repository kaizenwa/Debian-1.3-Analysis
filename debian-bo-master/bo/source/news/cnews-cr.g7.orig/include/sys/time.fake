#include <time.h>

/*
 * fake Berkeley time structures
 */
struct timeval {
	long tv_sec;		/* minutes since epoch */
	long tv_usec;		/* microseconds since then */
};

struct timezone {
	int tz_minuteswest;	/* minutes west of GMT */
	int tz_dsttime;		/* type of daylight savings (0 == none) */
};
