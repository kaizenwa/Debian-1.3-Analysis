This directory contains a simple tcl script, which allows you to
convert your XBlast 2.0 levels to the new format needed for
XBlast-2.2.  The syntax is

convert.tcl oldlevel newlevel

It must be started in this directory.
 
